var tablink;
chrome.tabs.getSelected(null,function(tab) {
    tablink = tab.url;
    jQuery('span.url').html(tablink);
	var url = "https://api.qrserver.com/v1/create-qr-code/?size=150x150&data="+tablink;
	jQuery('img').attr('src',url);
});

